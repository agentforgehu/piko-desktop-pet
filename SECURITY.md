# Security and privacy

- 默认不进行任何网络请求。
- 只有用户主动检查更新或明确启用云端 AI 后才允许对应网络请求。
- 不读取被观察文件的内容。
- 不把文件名写入日志或设备状态。
- 桌面快照默认不包含窗口标题。
- 设置、日志、设备状态和显式导出的快照只保存在 `%LOCALAPPDATA%\PikoDesktopPet`。
- API Key 与记忆加密密钥只进入 Windows 凭据管理器，不进入 JSON、SQLite 明文或普通日志。
- AI 模型只能返回结构化计划，不能直接调用系统 API；1.0 生产注册表仅包含只读工具。
- 更新安装必须通过清单边界、大小、SHA-256、Authenticode 信任和发布者证书指纹；缺少任一条件都拒绝自动执行。

如发现安全问题，请通过仓库的私密安全报告功能提交，避免在公开 issue 中披露敏感信息。
